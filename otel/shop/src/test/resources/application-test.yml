app:
  title: Welcome to SpringBootShop Tests!

management:
  tracing:
    enabled: false
  otlp:
    metrics:
      export:
        enabled: false

spring:
  cloud:
    discovery:
      client:
        simple:
          instances:
            orders:
            - uri: http://localhost:${wiremock.server.port}
            items:
            - uri: http://localhost:${wiremock.server.port}
